<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fit Genie</title>
</head>
<body>
  <h1>Fit Genie</h1>

  <div id="promptFormContainer">
    <div id="promptList">
      <!-- Prompts will be dynamically added here -->
    </div>
    <form id="promptForm">
      <label for="prompt">Enter your prompt:</label>
      <input type="text" id="prompt" name="prompt" required>
      <button type="button" id="submitBtn">Submit</button>
    </form>
  </div>

  <div id="responseContainer">
    <!-- Responses will be displayed here -->
  </div>

  <script src="controllers/api/submitPrompts.js" type="module"></script>
</body>
</html>